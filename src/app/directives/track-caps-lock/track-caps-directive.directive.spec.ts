import { TrackCapsDirectiveDirective } from './track-caps-directive.directive';

describe('TrackCapsDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackCapsDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
